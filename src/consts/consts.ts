/** マークダウンファイルの名前 */
export const MD_NAME = "index.md";

/** 画像ファイルのディレクトリ名 */
export const IMAGE_DIR = "/images";

/** ics-articlesの記事があるディレクトリ名 */
export const ENTRY_DIR = "/entry";

/** ログとログを分けるために使用します */
export const LOG_SEPARATOR = "-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-*-";

/** エラー時のログの頭に付与します */
export const LOG_ERROR = "[Error]:";

/** エラー以外のログの頭に付与します */
export const LOG_INFO = "[Info]:";
